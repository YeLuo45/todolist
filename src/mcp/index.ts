/**
 * MCP Entry Point - starts the MCP server
 */

// Import the server to initialize it
import './server'

// This file exists to make `npx tsx src/mcp/index.ts` work
console.error('[MCP] Starting TodoList MCP Server...')